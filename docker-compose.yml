version: "3.8"

services:
  bot:
    build: .
    env_file: .env
    ports:
      - "${REPORT_SERVER_PORT:-8080}:8080"
    volumes:
      - bot-data:/app/data
      - bot-static:/app/static
    restart: unless-stopped

volumes:
  bot-data:
  bot-static:
